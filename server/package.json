{
  "name": "mqtt-server",
  "version": "1.0.0",
  "description": "MQTT server/broker",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hankgalt",
  "license": "ISC",
  "dependencies": {
    "mosca": "^2.3.0"
  }
}
